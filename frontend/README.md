# Frontend Module

This module contains the Streamlit application for student placement prediction.

## Run the app

1. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```
2. Start the app:
   ```bash
   streamlit run frontend/app.py
   ```

## Features

- Interactive student feature inputs
- XGBoost prediction inference
- Model performance metrics display
- Dataset preview table
